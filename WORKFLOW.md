# 低 Token 产品需求工作流

## 1. 核心思路

目标不是减少必要分析，而是减少“高成本阶段的错误生成”。

```text
不确定性高                           不确定性低
需求分析 → 业务流程 → 页面 IA → 页面流程 → HTML → PRD
低成本                                               高保真
```

- 需求逻辑尚未确认时，不生成完整 HTML。
- 页面结构尚未确认时，不进入视觉精修。
- 原型尚未收敛时，不提前写完整 PRD。
- 已确认内容不重复设计，只做 Delta 更新。

## 2. 默认阶段与 Gate

| 阶段 | 核心问题 | 产物 | Gate |
| --- | --- | --- | --- |
| 业务诉求登记（按需） | 这件事是否值得进入正式需求 | 诉求记录 | 已决定转需求 |
| 需求分析 | 为什么做、谁在什么场景下遇到什么问题、做什么 | `01-需求分析.md` | 核心场景、范围、规则足够进入流程 |
| 业务流程 | 业务如何闭环 | `flows/...html` | 主流程和关键分支确认 |
| 页面 IA / Page Spec | 能力放在哪、页面怎么组织 | `prototype/workdraft/page-ia.md`、Spec | 页面结构足够稳定 |
| 页面流程（按需） | 用户如何跨页 / 多步骤完成任务 | `flows/...page-flow.html` | 关键操作路径确认 |
| HTML 原型 | 页面实际如何呈现 | HTML | 结构 / 交互确认 |
| Delta 迭代 | 哪些局部需要改 | 更新 Spec + HTML 片段 | 受影响模块重新确认 |
| PRD | 如何把已确认方案交给研发 | PRD + 补充信息 | 文档可执行、可验收 |
| 产品评审（按需） | 是否有关键产品质量缺口 | 评审记录 | P0 清零或明确阻塞 |

## 3. 最小上下文交接

```text
需求分析 → 业务流程
Sxxx + 相关 Fxxx + Dxxx + 必要 Qxxx

业务流程 → 页面 IA
已确认 Flow Spec / 流程路径 + 相关场景

页面 IA → 页面流程
Page ID + Module ID + 操作路径 + 相关 S/F/D

页面 IA / Page Spec → HTML
只读当前页面 Spec + Base + 1 个 Reference

原型迭代
只读受影响 Mxxx 的 Spec + HTML 标记片段

原型 → PRD
需求分析 + 已确认流程 + Page IA + Page Spec + 原型链接
HTML 不是业务事实源，PRD 无需通过重读 HTML 反推字段
```

## 4. Token 控制

- 源材料先建索引，再一次批量提取当前阶段需要的章节。
- 已消费内容写入稳定中间产物，下游不以“再确认一次”为由回读全文。
- 简单单页不画页面流程；无页面需求跳过 IA / HTML。
- 不生成独立“方案确认文档”复制需求分析和流程内容；已确认的分析、流程和 Page Spec 本身即方案基线。
- 页面组件使用稳定模块 ID 和 HTML 注释锚点，局部修改不重做整页。
- 产品评审只审当前 PRD 基线；发现问题后按责任范围最小回退，不重跑整条链路。
