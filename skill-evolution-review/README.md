# skill-evolution-review

这是一个**用户手工触发**的 Skill。
它是唯一允许读取 `_shared-wiki` 的 Skill；正常业务 Skill 对 Wiki 仅可在明确、可长期复用的方法反馈出现时做极简写入，不得读取。

用途：
1. 检查 `_shared-wiki` 中积累的稳定经验；
2. 判断哪些 Pattern 值得固化进现有 Skill；
3. 默认只给建议；
4. 只有用户明确授权后，才做最小 Patch。

建议每累计约 10～20 次真实 Skill 使用，或同类纠正明显重复时手工运行一次。
