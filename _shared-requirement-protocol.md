# 产品需求阶段产物与交接协议

本协议定义阶段顺序、最小产物、交接边界和低 Token 规则，不负责替代专业 Skill 执行。正常业务 Skill 不读取 `_shared-wiki`。

## 1. 默认链路与按需跳过

有页面的正式产品需求默认采用：

```text
业务诉求登记（按需）
→ 需求分析
→ 业务流程
→ 页面 IA / Page Spec
→ 页面流程（按需）
→ HTML 原型
→ Delta 迭代 / 锁定
→ PRD 编译 / 定稿
→ 产品评审（按需）
→ 归档（按需）
```

无页面需求可跳过页面 IA、页面流程与 HTML；简单单页需求可以跳过页面流程；已有等效确认材料时可直接进入对应阶段。不得为了“走完整流程”补造空产物。

- **独立调用**：完成用户指定的当前阶段即交付，不自动启动后续阶段。
- **串联调用**：仅在用户已明确要求连续完成多个阶段时继续交接；已有授权不重复询问。
- 保存目录不决定前置阶段。阶段准入只看当前任务需要的事实是否充分、有效、可追溯。
- 上游材料冲突时暂停受影响部分，要求用户指定有效基线；不自行选择业务事实。

## 2. 阶段责任与标准产物

| 阶段 / Skill | 足够输入 | 标准交付物 | 完成条件 |
| --- | --- | --- | --- |
| 诉求登记 / `business-request-registration` | 轻量业务想法、问题或建议 | 诉求记录与索引 | 用户确认处理结论；需要时标记转正式需求 |
| 需求分析 / `requirement-analysis` | 原始需求、已登记诉求或等效材料 | `01-需求分析.md` | Problem / Goal、核心场景、范围、核心规则和 Product Skeleton 足以进入流程 |
| 流程 / `business-flow-html` | 当前流程所需 S/F/D、Page Spec 或等效流程事实 | `flows/流程清单.md`、流程 HTML | 本次所需流程的主路径、关键分支和出口已确认 |
| 页面 IA / 原型 / `prd-html-prototype` | 已确认需求分析、必要业务流程或等效页面事实 | Page IA、Prototype Spec、HTML、原型清单 | 页面结构、必要页面流程和当前 HTML 已确认；支持模块级锁定 |
| PRD / `prd-writing` | 已确认需求分析、必要流程；有页面时优先使用已确认 Page IA / Spec / 原型链接 | 成对 PRD 主文档与补充信息 | PRD 不新增方案，已把确认结果结构化为可执行、可验收文档 |
| 产品评审 / `prd-product-review` | 指定 PRD 及可用补充材料 | `04-评审记录/产品专家评审.md` | 输出当前基线的 P0/P1/P2 与复审结论；完成评审不等于通过 |
| 归档 / `requirement-archiving` | 用户指定且已确认的当前结果集 | 已归档目录及 `00-需求信息.md` | 按实际执行范围核对并归档，不补造未执行阶段 |
| 手册 / `operation-manual-generator` | 用户指定的定稿事实源及必要参考资料 | HTML、来源 manifest、Fact Inventory | 事实可追溯且操作检查通过 |

不再维护独立 `02-方案确认.md` 作为默认阶段。需求分析、确认流程、Page IA / Spec 与原型共同构成逐步收敛的方案基线，避免重复写一份“方案摘要”。

## 3. 最小上下文交接协议

### 3.1 需求分析 → 业务流程

优先传：

```text
目标 Sxxx
+ 相关 Fxxx
+ 相关 Dxxx
+ 仍会影响流向的 Qxxx
+ 流程起止边界
```

不默认传完整聊天或全部附件。

### 3.2 业务流程 → 页面 IA / Spec

传已确认 Flow Spec / 流程路径、覆盖场景及必要 F/D；HTML 是视觉参考，不要求下游全文解析 HTML 才能理解流程。

### 3.3 页面 IA / Spec → 页面流程

只传目标 Page ID、Module ID、入口、动作、状态反馈和相关 S/F/D。页面流程确认后回写对应 Spec 的流程引用与状态。

### 3.4 Page Spec → HTML

HTML 阶段只读当前页面 Spec、公共 Base 和 1 个 Reference；业务定义充分时禁止重读需求分析、流程或原始材料。

### 3.5 原型 → PRD

PRD 优先消费：

```text
01-需求分析.md
+ 已确认流程事实 / Flow Spec
+ 已确认 page-ia.md
+ 已确认 Prototype Spec
+ 原型相对链接
```

HTML 仅是视觉交付，不作为字段、枚举、业务规则的唯一事实源。PRD 不应通过重新解析完整 HTML 反推需求。

### 3.6 评审 → 回退修订

- Problem / Goal / Scope / 核心业务规则：`requirement-analysis`
- 流程闭环 / 分支：`business-flow-html`
- Page IA / 交互 / 页面事实：`prd-html-prototype`
- PRD 表达、编号、结构：`prd-writing`

只回退受影响部分，不重跑整条链路。

## 4. 稳定编号与 Delta

需求分析维护：

- `Fxxx`：Fact
- `Sxxx`：Scenario
- `Dxxx`：Decision
- `Qxxx`：Open Question

页面阶段维护：

- `Pxxx`：Page
- `Mxxx`：Page Module（页面内稳定）

编号不复用。相同事实、场景或模块不重复创建。下游引用 ID 而不是复制长段正文。

原型已确认模块标记为 `LOCKED`。后续变更只读取和更新受影响 Mxxx；除非用户明确要求全局改版，不因局部修改重新设计锁定模块。

## 5. 保存与状态

正式需求默认目录：

`03_Requirements/进行中/REQ-YYYYMMDD-NNN-需求名称/`

按需使用：

```text
00-需求信息.md
01-需求分析.md
flows/
prototype/
03-PRD/
04-评审记录/
参考资料/
```

没有内容的目录和文件延迟创建。

`00-需求信息.md` 只维护恢复工作需要的最小索引：

```markdown
## 续作索引

- 当前目标：
- 当前阶段 / 下一动作：
- 可执行 Skill：
- 阻塞问题：无 / Qxxx
- 当前有效基线：
- 本轮受影响范围：Sxxx / Pxxx / Mxxx / PRD章节
- 按需读取入口：路径#章节

## 阶段状态

| 阶段 | 状态 | 有效产物 / 来源 | 确认或评审结论及适用基线 |
| --- | --- | --- | --- |
```

只保存当前值，不保存完整对话或阶段历史副本。基线变化时，将真正受影响的下游结果标为“待更新”，不机械标记全部阶段失效。

## 6. Token 控制硬规则

1. **索引优先**：大文档先读标题 / 元数据，再批量提取目标章节。
2. **一次消费**：形成 Fact Inventory、Flow Spec 或 Prototype Spec 后，后续默认不回读已消费源内容。
3. **阶段 Gate**：业务逻辑未稳定不做完整 HTML；页面结构未稳定不做视觉精修；有页面的需求原型未收敛前不默认写完整 PRD。
4. **Delta 修改**：局部变化只读受影响 S/P/M/章节和必要上下文。
5. **无重复文档**：不创建另一份内容高度重复的“方案确认”“阶段总结”只为了交接。
6. **按需流程**：页面流程、系统流程、评审等无必要时直接跳过并记录原因。
7. **不为复核回读**：校验优先对照已经生成的 Spec / Inventory，不以“再确认一下”为理由回读原始全文。
8. **清晰缺口优于猜测**：缺少会改变业务结论的事实时保留 Unknown / Qxxx，不用高成本生成来掩盖缺口。

## 7. 写入边界

- 各 Skill 只写自己的交付物及 `00-需求信息.md` 中与当前阶段相关的状态。
- 流程不改需求分析或 PRD；原型不改业务事实；评审不直接改 PRD。
- PRD 中流程和原型链接由 `prd-writing` 维护。
- 需要其他阶段修订时，给出目标 ID / 文件 / 章节、原因和影响；已有串联授权时可路由，否则交付缺口说明。

## 8. 版本与归档

- 流程：`flows/workdraft/` → `flows/最终确认版/`
- 原型：`prototype/workdraft/`（Page IA / Spec / HTML）→ `prototype/`（确认终态）
- PRD：`03-PRD/PRD-工作稿.md` + `PRD补充信息-工作稿.md` → `PRD-最终确认版.md` + `PRD补充信息-最终确认版.md`，始终成对并作为唯一当前确认终态
- 评审：一份记录维护当前所审基线和复审状态

工作稿和当前终态按共享规则维护。清理历史或过程材料前必须明确列出目标和影响并获得授权。归档不代表上线。
