# 产品需求阶段产物与交接协议

本协议定义阶段输入、交付物及交接边界，不负责执行任何阶段。正常业务 Skill 不读取 `_shared-wiki`。

## 1. 调用方式与保存位置

- **独立调用**：完成用户指定的当前阶段即交付，不自动启动其他阶段。
- **串联调用**：仅在用户已明确要求连续完成多个阶段时，在授权范围内交接并继续；已有授权不重复询问。确认本阶段内容不自动增加后续阶段的执行授权。
- 两种方式都可在 `03_Requirements/` 或用户指定位置运行。目录只决定保存位置，不决定必须经历哪些阶段。
- 接受上游有效产物，也接受用户直接提供的等效材料。阶段准入依据是完成当前任务必需的事实是否充分、有效且已确认，不要求材料由指定 Skill 生成或采用指定文件名。澄清可以从未确认的原始需求开始；评审可以检查用户指定的工作稿。
- 信息缺失时只问会改变当前结论的最小问题，不强制补跑前序阶段，不补造业务事实。外部材料与现有基线冲突时，请用户指定有效来源。

## 2. 阶段目标与交付物

| 阶段／责任 Skill | 足够输入 | 标准交付物 | 完成条件 |
|---|---|---|---|
| 澄清／`requirement-clarification` | 原始需求或待澄清材料 | `01-需求共创记录.md`；用户要求方案确认时生成 `02-方案确认.md` | 当前目标、范围、关键规则及路径明确，阻塞问题解决；方案确认仅校验必要的流程图 |
| 流程／`business-flow-html` | 已确认流程事实或相关方案／PRD章节 | `flows/流程清单.md`、流程 HTML | 本次所需流程已校验并由用户确认，不要求先生成共创或方案文档 |
| PRD／`prd-writing` | 已确认目标、范围、功能、规则及必要流程事实 | 成对主 PRD 与补充信息 | 内容经用户确认；只有明确判断为必要的流程图需要先确认 |
| 产品评审／`prd-product-review` | 指定 PRD 及可用补充材料 | `04-评审记录/产品专家评审.md` | 对指定基线输出评审结论、问题和必要复审结果；完成评审不等于通过 |
| 页面原型／`prd-html-prototype` | 用户确认可作为页面基线的最终 PRD | Prototype Spec 与页面 HTML | 页面校验和用户确认完成，不要求上游由本流水线执行 |
| 归档／`requirement-archiving` | 用户指定且已确认的当前结果集 | 已归档目录及 `00-需求信息.md` | 核对本次范围与必要产物后按授权归档；未执行或不适用阶段如实记录 |
| 手册／`operation-manual-generator` | 用户指定的定稿事实源及必要参考资料 | HTML、来源 manifest、Fact Inventory | 事实可追溯且操作检查通过；已上线手册需确认实际上线范围与文档一致 |

所有需求都判断流程图是否必要，允许“不需要”并写明原因；不生成图时仍需用文字定义必要的流程事实。首次独立编写 PRD 没有现成判断时，由 `prd-writing` 按相同标准判断，不要求调用澄清 Skill 留下一份证明。

## 3. 保存与状态

正式需求默认目录为 `03_Requirements/进行中/REQ-YYYYMMDD-NNN-需求名称/`，按需创建 `00-需求信息.md`、上述产物和 `参考资料/`。不创建无内容的前序文件。既有目录内沿用阶段路径；目录外使用用户指定位置，位置确实不明时再询问。

当前阶段 Skill 可创建或更新最小 `00-需求信息.md`，不为此调用另一个阶段。编号、名称沿用已确认信息，不能确定时只确认缺项。

```markdown
## 续作索引

- 当前目标：
- 执行范围：本阶段／用户已授权的串联阶段
- 当前阶段／下一动作：
- 可执行 Skill：
- 阻塞问题：无／Qxxx
- 当前有效基线：来源路径及确认状态
- 本轮受影响范围：
- 按需读取入口：路径#章节

## 阶段状态

| 阶段 | 状态 | 有效产物／来源 | 确认或评审结论及适用基线 |
| --- | --- | --- | --- |
```

阶段状态只维护与当前任务或交接有关的行，可用“未执行、进行中、待确认、已完成、不适用、待更新”。用户提供的材料注明“外部已确认输入”，不据此伪称前序阶段已执行。基线改变时标记受影响下游结果“待更新”，不把旧评审或确认结论套用于新内容。

索引只保存当前值；阶段结束、基线、阻塞点或下一动作变化时更新，不保存对话、推理或历史规则副本。目录外独立交付无需补建档案，但在交付说明中列明输入基线、完成状态、未决项和交接建议。

## 4. 写入与交接

- 各阶段只写自己的交付物及需求信息中的当前状态。澄清维护需求层面的流程必要性判断；流程 Skill 维护自己交付的清单、文件与确认状态。
- 需要修订其他阶段产物时，给出位置、原因和所需修改，交给责任 Skill。独立调用交付当前结果与缺口说明；已有串联授权时可继续路由，不由当前 Skill 代写。
- 评审不改 PRD；流程不改 PRD 链接；归档不补写业务正文。PRD 中流程、原型链接由 `prd-writing` 维护。
- 写入前读取现有索引和目标文件；发现并发更新或基线冲突时停止受影响写入。
- 下游只读取相关有效章节、来源与确认状态，复用标题索引、批量提取和已消费 Spec，不因换阶段默认全文重读。

## 5. 版本与归档

- 每类交付物维护一套工作稿与一套当前有效终态。工作稿直接修订；再次确认前保留旧终态，再次确认后替换，不累积过程版本。
- PRD：`03-PRD/PRD-工作稿.md`、`PRD补充信息-工作稿.md` → `PRD-最终确认版.md`、`PRD补充信息-最终确认版.md`，始终成对。
- 流程：`flows/workdraft/` → `flows/最终确认版/`；清单记录工作稿及当前确认状态。
- 原型：`prototype/workdraft/`（含 `spec/`）→ `prototype/` 与 `prototype/spec/`，同一页面的 Spec 和 HTML 同步固化。
- 替换已有终态需有本次内容确认；清理工作稿、旧版本或过程材料前列出目标、用途及影响并获得明确确认。尚未获准清理的文件标为待清理，不视为另一套有效基线。
- 既有文件名通过索引兼容，不因本协议升级批量改名、迁移或删除历史需求。
- 归档保留有效成果、最终评审结论及相关原始参考资料；过程材料仅在结论已被完整吸收且获准时清理，详见[归档规则](prd-requirement-lifecycle/references/workflow-archive.md)。归档不代表上线。

`prd-requirement-lifecycle` 仅为兼容路由入口；其目录中的共享参考和脚本可直接复用，不要求先执行该 Skill。
